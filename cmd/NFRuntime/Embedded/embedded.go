package main

// TODO Scan for .NFPackage files in its local directory and if there is only one open it
// by un-archiving to a temp directory and running it through the game validator and parser
// Saves are not stored in the game directory but in a specified directory based on the OS
// Since this is embedded if there are multiple .NFPackage files it should open up a small
// selection dialog unless a flag is passed specifying a specific package name
func main() {

}
